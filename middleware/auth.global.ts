export default defineNuxtRouteMiddleware((to, from) => {
  // console.log(to);
  // console.log(from);

  const isLogin = true;

//   if (!isLogin) {
//     return abortNavigation();
//   }

//   if (to.path !== "/") {
//     return navigateTo("/");
//   }


});
